#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: is number 1, b is number 2
 *
 * Return: integer value of the result
 */
int add(int, int)
{
	int result, a, b;

	result = a + b;

	return (result);
}
